{
  "name": "hidden-gems-monorepo",
  "private": true,
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{js,jsx}": "eslint --fix"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^13.0.0",
    "eslint": "^8.54.0",
    "prettier": "^3.0.0"
  }
}
